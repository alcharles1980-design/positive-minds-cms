# dependencies (pinned in package.json; reinstall with npm ci)
node_modules/
package-lock.json

# reconstructed-workspace scaffolding — symlinks that mirror the layout
# the six test scripts hardcode. Regenerated by `node tools/workspace.cjs`.
/v2
/ef
/content-api/
/generate-questions/
src/node_modules

# local IDE metadata
.idea/
